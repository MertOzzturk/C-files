#include <stdio.h>
#include <stdlib.h>

int main()
{
    int number;
    printf("Enter an integer \n");
    scanf("%d", &number);v

    if(number%10 == 0
    printf("%d is multiple of 10. \n")
    else
    print("%d is not multiple of 10.")
    return 0;
}
