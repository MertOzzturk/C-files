#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{
    int a,b,gdp,i;
    printf("bir sayý giriniz");
    scanf(" %d",&a);
    printf("2. sayi giriniz");
    scanf(" %d",&b);

    for(i=0;i<=a && i<=b;i++){
        if(i%a==0 && i%b==0);
            gdp=i;


    }
        printf(" %d",gdp);

    return 0;
}
